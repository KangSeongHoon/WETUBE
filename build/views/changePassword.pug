extends layouts/main

block content 
    .form_container
        form(action=`/users${routes.changePassword}`, method="post")
            input(type="password", placeholder="Current Password", name="oldPassword")
            input(type="passowrd", placeholder="New Pasword", name="newPassword1")
            input(type="password", placeholder="Verify New Password", name="newPassword2")
            input(type="submit", value="Change Password")
        